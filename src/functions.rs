pub mod functions {
  pub fn sum(a: i32, b: i32) -> i32 {
    a + b

    // The return keyword is optional
    // return a + b;

    // The last expression in a function is returned
    // a + b

    // The semicolon suppresses the return value
    // a + b;

    // The return keyword can be used to return early from a function
    // return a + b;
    
  }
}